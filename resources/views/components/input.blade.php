<div>
    <div class="form-group">
        <label for="">{{$title ??''}}</label>
        <input type="{{$type}}" name="{{$name ?? ''}}" placeholder="{{$placeholder ?? ''}}"
               class="{{$class ?? ''}}" id="{{$id ?? ''}}" value="{{$value??''}}">
    </div>
</div>
